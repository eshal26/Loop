# No database config needed right now — data lives in memory and resets
# when the server restarts. When Postgres is added later (see
# future-postgres/schema.sql), a DATABASE_URL will go here.

# AssemblyAI
ASSEMBLYAI_API_KEY="1e346819a085442090c04034d41d129a"

# App
PORT=4000
CLIENT_URL="http://localhost:5173"
